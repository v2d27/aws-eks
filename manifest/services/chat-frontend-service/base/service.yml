kind: Service
apiVersion: v1
metadata:
  name: hercules-frontend-service-main
spec:
  type: ClusterIP
  selector:
    app: hercules-frontend-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000