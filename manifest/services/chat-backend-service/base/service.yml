kind: Service
apiVersion: v1
metadata:
  name: chat-api-service
spec:
  type: ClusterIP
  selector:
    app: hercules-backend-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080